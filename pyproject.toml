[project]
name = "maze-generator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14.2"
dependencies = [
    "pillow>=12.2.0",
    "pydantic>=2.13.3",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["models*", "generator*"]
